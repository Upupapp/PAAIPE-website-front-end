# Tab 06 evidence — Concept UI

`/about` and `/programs`, captured 2026-09-03 from the production build.
Desktop 1440×900 (Chromium), mobile 390×844 (WebKit), full page, under
`reducedMotion: 'reduce'`.

Note the progress section on `/about`: it renders as prose with no figure,
counter or chart. Tab 06 forbids turning it into a numerical impact statement
until verified data exists, and a test asserts no digit appears there.
