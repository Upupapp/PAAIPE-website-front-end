# Tab 01 route-shell screenshots — Concept UI

Captured 2026-09-03 from the production build (`npm run build`) served by
`scripts/preview-server.mjs`.

- `desktop-1440x900-*.png` — Chromium at 1440×900
- `mobile-390x844-*.png` — **WebKit** at 390×844, `deviceScaleFactor: 2`

Mobile is captured in WebKit rather than Chromium because Chromium does not
reproduce iOS Safari layout and input behaviour.

These show **route shells only**. Page content is implemented in Tabs 02–10.
Label them **Concept UI** in any review material.
