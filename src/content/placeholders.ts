/**
 * Tab 01 scaffolding ONLY.
 *
 * A statically built dynamic route needs at least one path to exist, or the
 * template cannot be direct-loaded and the acceptance check cannot be proved.
 * These slugs are deliberately self-describing so they can never be mistaken
 * for approved content, and the pages they render are marked `noindex`.
 *
 * Tab 03 deletes this file and replaces it with the typed content registries.
 */
export const PLACEHOLDER_EVENT_SLUGS = ['template-preview'] as const;
export const PLACEHOLDER_RESOURCE_SLUGS = ['template-preview'] as const;
